# Standards Roadmap (high level)

We track conformance with:
- WPT (Web Platform Tests), curated subsets per subsystem
- test262, curated subsets expanding over time

See `plan_progress.md` for milestones and checklists.

